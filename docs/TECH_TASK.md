# Техническое задание — SMS Forwarder (Android → Telegram)

**Проект:** sms-forwarder
**Дата:** 2026-09-07
**Статус:** черновик на согласование
**Версия:** 0.1

---

## 1. Назначение

Android-приложение, которое пересылает **входящие SMS** и **пропущенные вызовы**
с телефона в **Telegram** через бота. Работает в фоне без заметных уведомлений,
не выгружается системой. Весь текст приходит в чат пользователя.

## 2. Термины

| Термин | Значение |
|---|---|
| **Бот** | Telegram-бот, созданный через @BotFather (выдаёт токен) |
| **MTProto** | Протокол Telegram (последняя версия схемы) |
| **Web-proxy** | HTTP/SOCKS5/MTProto-прокси для доступа к Telegram (обход блокировок) |
| **FG-сервис** | Foreground Service — фоновый сервис Android, защищённый от выгрузки |
| **Chat ID** | Идентификатор чата, куда слать сообщения (обычно ID владельца) |

## 3. Функциональные требования

### 3.1. Пересылка SMS
- Перехват **входящих SMS** в реальном времени (BroadcastReceiver `SMS_RECEIVED`).
- Для каждой: отправитель (номер и/или имя из контактов), текст, дата/время.
- Формат сообщения в Telegram:
  ```
  📩 SMS [2026-09-07 21:35]
  От: +7 900 123-45-67 (Имя)
  ---------------------------------------------------------------
  Текст сообщения…
  ```
- Опциональные фильтры (задел): только контакты / только определённые номера /
  регулярные выражения / чёрный список коротких номеров (банки, реклама).

### 3.2. Пропущенные вызовы
- Отслеживание входящих вызовов, которые **не были приняты** («пропущенные»).
- Для каждого: номер, имя из контактов, дата/время.
- Формат в Telegram:
  ```
  📵 Пропущенный [2026-09-07 21:33]
  От: +7 900 123-45-67 (Имя)
  ```
- Не пересылаются: принятые/исходящие вызовы, сброшенные пользователем
  (настраиваемо — см. раздел «Открытые вопросы»).

### 3.3. Настройки в UI приложения
Экран настроек (один, минималистичный):
- **Токен бота** (обязательное поле, ввод-пароль, сохранение в шифрованном виде).
- **Chat ID** (числовое поле; автоопределение по кнопке «Получить мой ID», см. 3.4).
- **Прокси** (опционально): тип (HTTP / SOCKS5 / MTProto-proxy), host, port,
  логин/пароль (опционально). Кнопка «Проверить подключение».
- **Переключатели**: «Пересылать SMS», «Пересылать пропущенные», 
  «Не пересылать SMS от коротких номеров», «Режим роуминга» (задел).
- **Статус сервиса**: «Работает / Остановлен», кнопка запуска/остановки,
  счётчик пересланных сообщений, лог последних событий (10 строк).

### 3.4. Проверка подключения и авто-ID
- Кнопка «Проверить подключение»: отправляет тестовое сообщение
  `✅ SMS Forwarder работает` и показывает результат (успех / ошибка с текстом).
- Кнопка «Получить мой ID»: приложение отправляет боту команду и показывает
  полученный chat_id (пользователю достаточно написать боту `/start` один раз).

### 3.5. Запуск и работа в фоне
- **Автозапуск** с включением телефона (BootReceiver, `BOOT_COMPLETED`).
- **Foreground Service** (тип `dataSync` / `specialUse` на новых Android) — 
  приоритет против выгрузки, `START_STICKY` (перезапуск при убийстве).
- Повторная отправка при временном отсутствии сети (очередь, ретраи с backoff).
- **Без всплывающих уведомлений**:
  - канал уведомления FG-сервиса с важностью `IMPORTANCE_MIN`
    (невидимое уведомление; на Android 13+ пользователь отключает канал — 
    сервис продолжает работать);
  - никаких тостов/диалогов при пересылке;
  - опция «Отключить все уведомления» на Android 13+ (запрос `POST_NOTIFICATIONS`
    не показывается / отклоняется).

## 4. Нефункциональные требования

- **Минимальная версия Android:** 8.0 (API 26). **Target:** 34 (Android 14).
- **Язык:** Kotlin. **Сборка:** Gradle (Kotlin DSL).
- **Конфиденциальность:** SMS и номера не покидают устройство иначе как через
  Telegram-бота; текст не логируется на устройстве (только метаданные последних
  10 событий в памяти). Токен хранится в `EncryptedSharedPreferences`.
- **Надёжность:** очередь недоставленных сообщений с ретраями
  (экспоненциальный backoff, кап 5 минут), работа без сети не теряет события.
- **Размер:** APK ≤ 25 МБ (с TDLib — см. §7, риск роста до 40–60 МБ).
- **Ограничение по батарее:** приложение запрашивает исключение из
  оптимизации батареи (`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) +
  инструкция для вендорных прошивок (Xiaomi/Huawei/Samsung — ручное разрешение
  автозапуска), т.к. без этого «не выгружаться» на всех устройствах невозможно.

## 5. Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│  UI (MainActivity + Settings)                               │
│  - токен бота, chat id, прокси, переключатели, статус       │
└──────────────┬──────────────────────────────────────────────┘
               │ (EncryptedSharedPreferences)
┌──────────────▼──────────────────────────────────────────────┐
│  SMSReceiver  │  CallReceiver  │  BootReceiver               │
│  (SMS_RECEIVED)│ (PHONE_STATE + │  (BOOT_COMPLETED → старт   │
│               │  CallLog)      │   сервиса)                  │
└──────────────┬─────────────────┼────────────────────────────┘
               │                 │
┌──────────────▼─────────────────▼────────────────────────────┐
│  ForwardService (Foreground, START_STICKY)                  │
│  ├─ QueuedEvents (недоставленные → retry с backoff)          │
│  └─ TelegramClient (интерфейс)                               │
│        ├─ TdlibClient  — MTProto через TDLib (+ proxy)      │
│        └─ BotApiClient — резервный: Bot API HTTPS (+proxy)  │
└─────────────────────────────────────────────────────────────┘
```

- **SMSReceiver** — `RECEIVE_SMS`, срабатывает на каждое входящее SMS
  (не требует статуса Default SMS Handler).
- **CallReceiver** — `READ_PHONE_STATE` + `READ_CALL_LOG`: вызов звонил
  (`RINGING`) и завершился без ответа → событие «пропущенный».
- **ForwardService** — единственный владелец сетевого стека и очереди.
- **TelegramClient** — интерфейс с двумя реализациями (§7).

## 6. Права (permissions)

| Право | Назначение |
|---|---|
| `RECEIVE_SMS` | перехват входящих SMS |
| `READ_PHONE_STATE` | состояние вызова (звонок/сброс) |
| `READ_CALL_LOG` | подтверждение «пропущенный» (имя/тип) |
| `READ_CONTACTS` | имя отправителя/звонящего из контактов (обрабатывается на устройстве) |
| `INTERNET` | отправка в Telegram |
| `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_DATA_SYNC` | фоновый сервис (Android 14+) |
| `POST_NOTIFICATIONS` (Android 13+) | канал уведомления FG — пользователь может отключить |
| `RECEIVE_BOOT_COMPLETED` | автозапуск после перезагрузки |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | исключение из Doze |

## 7. MTProto + прокси (ключевое требование)

### 7.1. Основной путь — TDLib (MTProto)
- **TDLib** (Telegram Database Library) — официальная библиотека Telegram,
  работает по **MTProto** и всегда поддерживает **последнюю версию схемы**.
- Авторизация бота в TDLib: `setAuthenticationToken(bot_token)` 
  (поддерживается для ботов).
- Прокси: `addProxy` + `setProxy` — типы **HTTP / SOCKS5 / MTProto-proxy**
  (то самое «web proxy» для обхода блокировок).
- Минусы: нативная библиотека (NDK/CMake), размер APK растёт до 40–60 МБ,
  сложнее сборка. Сборку можно автоматизировать в GitHub Actions (готовый
  prebuilt TDLib Android AAR).

### 7.2. Резервный путь — Bot API HTTPS
- Классический `api.telegram.org` (OkHttp) с поддержкой HTTP/HTTPS/SOCKS5
  прокси на уровне OkHttp.
- Проще и легче, НО это **не MTProto**. Используется как fallback
  (если TDLib-путь недоступен) и как путь для проверки подключения.

### 7.3. Выбор при согласовании
| Критерий | TDLib (MTProto) | Bot API + прокси |
|---|---|---|
| MTProto последней версии | ✅ да | ❌ нет |
| Web-proxy (HTTP/SOCKS5) | ✅ | ✅ |
| Размер APK | 40–60 МБ | 5–8 МБ |
| Сложность сборки | высокая (NDK) | низкая |
| Надёжность для ботов | высокая, но тяжёлая | отличная, стандарт |

**Рекомендация:** сделать TDLib основным (требование «MTProto последней версии»),
Bot API — резервным режимом. Разрешить выбор режима в настройках.

## 8. UX — экраны

1. **Экран настроек** (главный): токен, chat id, прокси, переключатели, кнопки
   «Проверить», «Получить мой ID», статус сервиса.
2. **Экран Onboarding** (первый запуск): 3 шага — «создай бота у BotFather» →
   «вставь токен» → «напиши боту /start и нажми "Получить мой ID"».
3. **Экран статуса/лога** (последние события, счётчики).

## 9. Этапы разработки (план)

| Этап | Содержание | Результат |
|---|---|---|
| **0. Согласование ТЗ** | этот документ | ✅ здесь |
| **1. Скелет** | проект Gradle/Kotlin, экраны, настройки, шифрованное хранилище | приложение-каркас |
| **2. Захват событий** | SMSReceiver + CallReceiver + фильтры + очередь | пересылка в лог |
| **3. Отправка** | Bot API + прокси (сначала), потом TDLib | реальная пересылка в Telegram |
| **4. TDLib (MTProto)** | подключение TDLib, прокси, выбор режима | MTProto-путь |
| **5. Фон и «невидимость»** | FG-сервис, автозапуск, battery-исключение, инструкции вендоров | стабильная работа |
| **6. CI-сборка** | GitLab CI/CD: сборка APK (в т.ч. TDLib) на push + семантические теги/релизы | готовый APK-артефакт + релиз vX.Y.Z |
| **7. Полировка** | иконка, onboarding, тест на реальном устройстве | релиз-кандидат |

## 10. Ограничения и риски (честно)

- **Android 14+ (target 34):** FG-сервисы с типами `dataSync`/`specialUse`
  требуют явных разрешений; «невидимость» ограничена — уведомление сервиса
  нельзя убрать полностью на Android 8–12 (только минимальная важность),
  на 13+ — пользователь отключает канал вручную.
- **Вендорные прошивки (Xiaomi, Huawei, Samsung):** система может убивать
  сервис, пока пользователь не разрешит «Автозапуск» в настройках вендора.
  Это решается инструкцией в onboarding + `START_STICKY` (не гарантирует
  на 100% на MIUI/HyperOS — известная боль всех таких приложений).
- **SMS:** на Android 10+ чтение входящих SMS через receiver работает всегда
  (не требует default handler); чтение *истории* SMS требует `READ_SMS`
  (в это ТЗ не входит, только входящие).
- **Блокировки Telegram:** при блокировке api.telegram.org MTProto-прокси
  или HTTP/SOCKS5-прокси решают; качество зависит от выбранного прокси.
- **Размер APK с TDLib** 40–60 МБ — приемлемо для личного использования.

## 11. Открытые вопросы (на согласование)

1. **MTProto обязателен или достаточно Bot API + прокси?**
   (TDLib = +40МБ и сложная сборка; но требование понято как «обязателен».
   Рекомендация: TDLib основным + Bot API резервным.)
2. **Прокси**: HTTP/SOCKS5 достаточно? Нужен ли именно MTProto-proxy тип?
3. **Пропущенные вызовы**: пересылать только «не принял и сбросил звонящий»,
   или также «сбросил абонент»? (по умолчанию: только реально пропущенные)
4. **Фильтры SMS** в первой версии: нужен ли чёрный список (банки/реклама)
   сразу или позже?
5. **Минимальная Android**: 8.0 (API 26) ок?
6. Готов ли APK 40–60 МБ (TDLib) — или предпочитаешь лёгкий вариант?
7. **Сборка**: собирать APK в GitHub Actions (артефакт) — ок?
8. Название репозитория: `ozyab09/sms-forwarder`?
---

## 12. Согласованные решения (дефолт, без ответа — best judgment)

1. **MTProto:** TDLib (основной) + Bot API HTTPS (резервный режим). В настройках — переключатель режима.
2. **Прокси:** все три типа — HTTP, SOCKS5, MTProto-proxy.
3. **Пропущенные вызовы:** только реально пропущенные (звонил и не дождался ответа).
4. **Фильтры SMS:** в первой версии — чёрный список коротких номеров (банки/реклама) включён; детальные regex-фильтры — позже.
5. **Минимальная Android:** 8.0 (API 26), target 34.
6. **APK 40–60 МБ** с TDLib — приемлемо (личное использование).
7. **CI:** GitLab CI/CD (`.gitlab-ci.yml`), APK-артефакт на каждый push + pipeline на теги semver.
8. **Репозиторий:** `ozyab09/sms-forwarder` (создать после согласования).

Статус: **ТЗ v0.1 — на согласовании**. После подтверждения — этап 1 (скелет проекта).


---

## 13. CI/CD — GitLab + SemVer (добавлено по требованию)

### 13.1. Пайплайн (`.gitlab-ci.yml`)
Этапы на каждый push в ветки и на теги:

| Этап | Что делает |
|------|-----------|
| `build` | Сборка debug APK (Gradle `assembleDebug`), кэш Gradle между джобами |
| `test` | Юнит-тесты (`testDebugUnitTest`) + lint (`lintDebug`) |
| `release` (только теги `v*`) | Сборка **release APK** (`assembleRelease`), подпись (ключ из CI-переменных), публикация артефакта |
| `publish` (только теги `v*`) | Автосоздание **GitLab Release** из тега с APK + changelog |

Правила запуска:
- `push` на любую ветку → `build` + `test`
- `push` тега `vX.Y.Z` → `build` + `test` + `release` + `publish`
- Merge request → `build` + `test` (защита: pipeline должен быть зелёным до merge)

### 13.2. SemVer (семантическое версионирование)
- Теги строго вида **`v<major>.<minor>.<patch>`** (пример: `v1.2.0`).
- Правила инкремента:
  - **major** — ломающие изменения (смена протокола, target API, архитектуры);
  - **minor** — новые функции (новый тип прокси, фильтры SMS, режим);
  - **patch** — фиксы и полировка (баги, переводы, иконка).
- Версия подтягивается из тега автоматически:
  `versionName = git describe --tags` / переменная CI `CI_COMMIT_TAG`.
- `versionCode` — монотонный счётчик (считается из semver: major*10000 + minor*100 + patch).

### 13.3. GitLab Release
- На каждый тег `v*` пайплайн создаёт **Release** (GitLab Releases API):
  - имя: `vX.Y.Z`;
  - описание: автогенерация changelog (из commit messages между предыдущим тегом и текущим);
  - ассеты: release APK (`app-release.apk`), checksum (SHA-256).
- `CHANGELOG.md` — разделы `## [vX.Y.Z] - YYYY-MM-DD` (обновляется в том же MR, где bump версии).

### 13.4. Переменные CI (настройка в GitLab)
| Переменная | Назначение |
|---|---|
| `KEYSTORE_BASE64` | Подписывающий keystore (base64) |
| `KEYSTORE_PASSWORD` | Пароль keystore |
| `KEY_ALIAS` | Алиас ключа |
| `KEY_PASSWORD` | Пароль ключа |

Никакие секреты в репозиторий не попадают — только через CI vars (masked).
Токен бота в приложении — это runtime-настройка пользователя (UI), не CI.

### 13.5. GitLab Runner
- Требуется runner с Android SDK (можно shared runner GitLab.com с docker-образом
  `android:latest` — стандартный способ).
- Кэш: `~/.gradle` (gradle-wrapper), `~/.android` (SDK license) между джобами.
